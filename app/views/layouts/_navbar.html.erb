<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">iShopBeer</a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Dashboard", dashboard_path %></li>
        <li><%= link_to "All Beers", beers_path %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Browse Styles<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "All", styles_path %></li>
            <% @styles.each do |style| %>
            <li><a href="/<%= style.name.parameterize %>"><%= style.name %></a></li>
            <% end %>
          </ul>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <div class="navbar-form navbar-left">
          <%= form_tag(beers_path, method: :get) do %>
          <div class="form-group">
            <%= label_tag :q %>
            <%= text_field_tag :q, nil, class: "form-control", placeholder: "Search Beers" %>
            <%= submit_tag "Search", class: "btn btn-default" %>
          </div>
        </div>
        <% end %>
        <li><a href="/cart/" id='cart'><span class="badge"><i class="glyphicon glyphicon-shopping-cart"></i> <%= get_total %></span></a></li>
      <% if logged_in? %>
        <li class="dropdown" id='login'>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.email %><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Past Orders", orders_path %></li>
            <li><%= link_to "Logout", logout_path, method: :delete %></li>
          </ul>
          <% else %>
          <li><%= link_to "Login", login_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
